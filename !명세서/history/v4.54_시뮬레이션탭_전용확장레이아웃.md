# v4.54 시뮬레이션 탭 전용 확장 레이아웃

작성일: 2026-04-13

## 변경 요약
- `시뮬레이션` 탭 진입 시 좌측 가계도 사이드바와 우측 가이드를 자동으로 닫는다.
- `시뮬레이션` 탭 본문 폭을 `1080px` 고정에서 `1480px`로 확장한다.
- 본문 좌우 패딩도 줄여, 흐름 카드/테이블이 화면을 최대한 넓게 쓰도록 조정한다.

## 의도
- 시뮬레이션은 집중해서 보는 화면이므로 보조 패널보다 본문 폭 확보가 우선이다.
- 기존 고정 폭은 사건형 시뮬레이션을 보기에 답답했고, 중앙 여백이 과하게 남았다.
